import React from 'react'

// Separate button components insted of passing color in as a prop all over the place
const PurpleButton = () => {
	return (
		<div>
			<button className='purple-button'>BUTTON BUTTON</button>
		</div>
	)
}

export default PurpleButton
